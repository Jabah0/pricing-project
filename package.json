{
  "devDependencies": {
    "@types/passport-local": "^1.0.38",
    "turbo": "^1.13.3"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "node apps/api/dist/src/main"
  },
  "dependencies": {
    "@lottiefiles/dotlottie-solid": "^0.0.9",
    "bcrypt": "^5.1.1",
    "date-fns": "^3.6.0"
  }
}
